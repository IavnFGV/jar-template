plugins {
    id "java"
}

group = "com.example"
version = "1.0.0"

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = "UTF-8"
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation "org.junit.jupiter:junit-jupiter:5.10.2"
}

test {
    useJUnitPlatform()
}

jar {
    archiveBaseName = "solution"
    archiveVersion = ""

    manifest {
        attributes(
                "Main-Class": "Main"
        )
    }

    // Required by the task: jar must contain source code.
    from("src/main/java") {
        into("source")
        include("**/*.java")
    }
}
